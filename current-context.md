# Current Context — Cocktail Library

## Phase & chunk

Agreed phase plan (revised by user on 2026-08-15 — private recipe CRUD moved into Phase 3 with recipes; publish/unpublish moderation moved to Phase 5 with admin tools):

1. ~~Inspect repository, reconcile continuity files~~ — done
2. ~~Convert TypeScript scaffold to JavaScript; app shell, routing, env handling, design tokens (Phase 0)~~ — **done, 2026-08-15**
3. ~~DB migrations, seed taxonomies, profiles/memberships/invitations/roles, RLS (Phase 0)~~ — **done, 2026-08-15**
4. ~~Auth + invitation redemption (Phase 1)~~ — **done and verified, 2026-08-16**
5. ~~Ingredient/product catalog + private My Bar (Phase 2)~~ — **done, 2026-08-16**
6. ~~Recipes, private recipe CRUD, components, substitutions, families, relationships (Phase 3)~~ — **done and verified, 2026-08-16**
7. ~~Unit preference + conversion (Phase 3)~~ — **done, 2026-08-16 — verified**
8. ~~Availability engine, tested (Phase 3)~~ — **done, 2026-08-16 — verified**
9. ~~Library browsing, filters, Favorites, Want to Make (Phase 4)~~ — **done, 2026-08-16 — verified**
10. ~~Purchase recommendations, tested (Phase 4)~~ — **done, 2026-08-16 — verified**
11. ~~Recipe publishing + admin unpublishing (Phase 5)~~ — **done, 2026-08-16 — DB-verified, not yet browser-verified**
12. Admin catalog tools + JSON import preview/validation (Phase 5) — **next**
13. Responsive/accessibility/security/deployment QA (Phase 6)

Each numbered step is a development chunk boundary for this file.

## Last completed chunk

Step 11 — recipe publishing and admin unpublishing, wiring up `DetailScreen`'s previously-inert "Publish" button and rebuilding `AdminScreen`'s moderation tab off real data.

**Database**: `20260815230002_recipe_publishing.sql` — `publish_recipe(p_recipe_id)` and `unpublish_recipe(p_recipe_id)`, both `SECURITY DEFINER` (same reasoning as `redeem_invitation`: the `recipes` UPDATE grant deliberately excludes `visibility`/`moderation_status` since step 6, so no direct client update could do this anyway). `unpublish_recipe` handles two distinct spec-defined actions in one function based on caller identity: the owner can self-unpublish (→ `moderation_status: 'active'`, since it's not a moderation action), or an admin can unpublish someone else's recipe (→ `moderation_status: 'unpublished_by_admin'`). Added a `published_at` timestamp column, set on publish, used by the moderation UI. Also added `recipe_id` column... no wait, added `published_at` only — one column.

**A real bug found and fixed before shipping**: the initial authorization check (`if not (v_recipe.owner_id = auth.uid() or public.is_admin()) then raise ...`) is unsafe for classic recipes, where `owner_id` is `NULL`. `NULL = auth.uid()` evaluates to `NULL`, and PL/pgSQL's `IF` treats a `NULL` condition the same as `false` — skips the branch — meaning the exception would silently *not* fire and a non-admin could slip through. Fixed with `coalesce(v_recipe.owner_id = auth.uid(), false)` to force a real boolean. Caught by reasoning through the SQL before pushing, not by a failing test — then **verified for real**: created a throwaway test recipe, simulated the admin's `auth.uid()` via `set_config('request.jwt.claims', ...)` in a `db query` session (since `db query` runs as postgres directly, with no JWT context otherwise) to confirm publish/unpublish both work correctly for an owner, then simulated a random unrelated user id attempting to unpublish the real Negroni (a classic, `owner_id IS NULL`) — correctly rejected with "Not authorized," and Negroni's row was confirmed untouched afterward. Test recipe cleaned up.

**Client**: `src/services/recipes.js` gained `publishRecipe`/`unpublishRecipe`/`fetchCommunityRecipes`. `DetailScreen.jsx`: the "Publish" confirm dialog now actually calls `publishRecipe` + `refetchRecipes`; added a parallel "Unpublish" action (shown when `c.source === 'community'` and the viewer is the owner or an admin) with a confirm dialog whose wording differs by who's doing it. `AdminScreen.jsx`'s Moderation tab now fetches real published community recipes and unpublishes for real; the mock's invented "pending/approve/reject" pre-publish review workflow was removed entirely rather than preserved, since the spec has no such step (publishing is immediate) — this included dropping the Overview tab's "Pending Review" card, which referenced the same fictional status. Overview's "Community Recipes" stat now reuses the real fetched count; the other three overview stats and the Invitations/Batch Import tabs are still mock, explicitly deferred to step 12 ("admin catalog tools").

**Follow-on, same day**: user reported no visible way to edit a recipe. Spec §4/§7.5 already scope this ("Edit own private recipes: Yes" for both roles, "Manage classic shared recipes: Yes" admin-only, "Edit another user's recipe: No by default" for admin too) so this wasn't a new scope decision, just a real gap (`current-context.md` had been carrying "real recipe editing UI" as a known-missing item since step 6). While building it, found the DB was actually **more permissive than the spec**: the step-6 `recipe_is_editable()` function and the `recipes` UPDATE/DELETE policies read `owner_id = auth.uid() or is_admin()`, which let an admin edit or delete *any* member's private recipe - never exercised by any UI, but a real standing gap between spec and schema. Migration `20260815231800_tighten_recipe_edit_scope.sql` tightens all three to `owner_id = auth.uid() or (owner_id is null and is_admin())`, scoping admin edit/delete rights to the ownerless classic catalog only. `unpublish_recipe()` (a separate function) is unaffected - admin moderation of a *published* community recipe is still spec-correct as "Yes". Verified directly: simulated the admin's auth context and evaluated the exact updated predicate against three owner_id cases (own recipe → true, classic/null → true, a different member's recipe → false) - confirms the fix without needing a second real profile row (only one exists in this dev project).

Client-side: added `updateRecipe()` to `src/services/recipes.js` (update recipe row, then delete-and-reinsert components/tags, same shape as `createRecipe`, no rollback-on-partial-failure since there's nothing sensible to roll back to). Generalized `EditorScreen.jsx` to double as an edit form: new route `/library/:id/edit`, prefills from `computed` + reverses the amount/unitLabel encoding, gates on `isOwner || (isAdmin && source === 'classic')` client-side (the DB policy is the real gate). Added an "Edit Recipe" button to `DetailScreen.jsx` under the same condition. Also fixed stale copy in `EditorScreen.jsx` left over from before step 11 ("Publishing to the community isn't available yet").

**Second follow-on, same day, from real browser testing**: user reported (1) publish worked, (2) Admin Moderation showed it, (3) unpublishing from the detail page "still appears as published", (4) "I can only unpublish in the recipe itself" (Admin panel's own Unpublish felt broken), (5) long recipes hide the Edit button under the bottom nav, and the Edit *form*'s Save button is similarly unreachable ("some elements are hidden and I can't save"). Two real bugs, found by reading the code (no browser access in this environment - fixes are code-reasoned, not yet re-verified by the user):

1. **`useRecipes.js`'s `load()` set `loading: true` on every call, including `refetchRecipes()`** - and `AppShell`'s `isLoading` check (`catalog.loading || inventory.loading || recipesLoading || lists.loading`) unmounts the entire `Outlet` in favor of a bare "Loading..." screen whenever any of those are true. So every publish/unpublish/create/edit was flashing the whole app to a blank screen and back, destroying the current screen's local state (dialogs, scroll position) each time - very likely what made publish/unpublish look like it silently "didn't work" from the user's side, even though the underlying RPC calls were fine (independently DB-verified earlier). This is the exact same class of bug already found and fixed for My Bar toggles in step 5/6 (`useInventory.js`/`useLists.js`, via optimistic updates); `useRecipes.js` never got the equivalent fix. Fixed by simply not re-entering `loading: true` after the first mount - `recipes` state isn't cleared during a refetch, so there's no need to hide the screen while it refreshes.
2. **Bottom nav clearance**: `BottomNav` was a flat `height: 64` with no `env(safe-area-inset-bottom)` accounting, and all nine screens using the shared `paddingBottom: 80` bottom-content-clearance convention had the same gap. Fixed by making `BottomNav` grow with the safe-area inset, and bumping all nine screens' clearance to `calc(96px + env(safe-area-inset-bottom, 0px))` for consistency (not just `DetailScreen`/`EditorScreen` - the same gap existed everywhere, it just hadn't been hit yet on shorter screens).

**Third follow-on, same day**: user reported the previous two fixes didn't help - screenshots showed long content (Mojito's steps, the Library grid) cut off flush against the bottom nav with **no scrollbar anywhere**, not just insufficient clearance. That "no scrollbar at all" detail was the real tell: `AppShell`'s root wrapper (`src/App.jsx`) used `minHeight: "100dvh"` on the flex row containing the `overflowY: "auto"` content div. That's a classic flexbox trap - a flex container with only `minHeight` (not `height`) has no *definite* height, so a flex child can't establish a bounded box to scroll within; it just grows to fit its content instead, and `overflow-y: auto` never actually engages. The whole app was silently never scrolling anywhere, at any screen, for any content taller than the viewport - the padding-bottom fix from the previous chunk was correct but moot, since there was no scroll container for it to create clearance within. Fixed by changing `minHeight` to `height` on that one wrapper, which gives the inner scroll div a real bounded height. This is the standard "app shell: fixed nav + internal scroll region" pattern and is almost certainly the actual, complete fix - still not personally re-verified in a browser (no browser access in this environment). **User confirmed this fix worked.**

**Taxonomy chunk, same day, user-initiated design questions (not a bug report)**: user asked why every juice needs its own type, whether sugar needs a branded product, and whether browsing "Spirit" would dump every rum/whiskey style into one undifferentiated list as the catalog grows. Answered from the spec (§4/§7.2: members can't create ingredient types, only products mapped to existing ones; §10.1: no fuzzy matching, so distinct-tasting ingredients need distinct types) and pointed out the real gap: `ingredient_types.parent_type_id` already exists in the schema for exactly this (`Spirit → Gin → London Dry Gin`, per its own column comment) but was never populated or used anywhere. User agreed to use it (parented under Spirit, not split into sibling categories) and asked for category display order (Spirits first; Bitters/Herb/Juice last) and to fold the sparse "Citrus" category into a broader "Juice" one.

Migration `20260816010047_category_order_and_spirit_hierarchy.sql`: adds `ingredient_categories.sort_order`, sets it so Spirit leads and Bitters/Herb/Juice trail; adds "Rum" and "Whiskey" as mid-level `ingredient_types` under Spirit and reparents Dark Rum/Bourbon beneath them via `parent_type_id`. **Surprise found mid-migration**: a "Juice" category (and an unrelated, still-empty "Wine" category) already existed in the live DB with no migration behind them - something created them outside the tracked migration history, which `AGENTS.md` explicitly calls out as a thing to avoid ("no undocumented dashboard-only changes"). Worth asking the user about at some point, but not investigated further here - "Wine" was left untouched (out of scope, no types on it yet), and the migration merges "Citrus" into the existing "Juice" row instead of renaming (renaming collided on the unique `name` constraint).

**Second surprise, found while fixing the client**: `fetchIngredientTypes()` (`src/services/catalog.js`) never selected `parent_type_id` in the first place - meaning `resolveOwnedIngredientTypes()`'s parent-walk (`src/domain/availability.js`, built and unit-tested back in step 8) has silently never worked against real data; it only ever appeared to work because its unit tests hand the function mock objects that already carry `parent_type_id`. Fixed by adding the column to the select alongside the taxonomy change - this was a latent, real correctness gap independent of anything the user asked about this chunk, just surfaced by needing the same field for the new My Bar grouping UI.

Client changes: `fetchIngredientCategories()` now orders by `sort_order, name` instead of plain `name`. `MyBarScreen.jsx` now sub-groups each category's type list by `parent_type_id` - a parent type's row is followed immediately by its (filtered) children, indented and slightly de-emphasized; a child that matches a search but whose parent doesn't (e.g. searching "dark") still renders, just flat, so grouping logic can never hide a real search match. `AddProductScreen.jsx` and `LibraryScreen.jsx` don't do category-based grouping (the former is a flat name-match datalist, the latter doesn't touch ingredient types at all), so neither needed changes.

**Immediate follow-up, same session, from a screenshot of the result**: three things. (1) User noticed "Rum" showed as not-owned while "Dark Rum" (its child) showed owned, and asked whether that's even coherent - it is (two independent ownership facts per spec §8.1: a generic entry and a specific one both independently satisfy the type), but the toggle being "off" while a child is "on" reads as a contradiction without explanation. Fixed by adding a small "Covered by Dark Rum" note under a parent row whenever it's not directly owned but a child is - purely additive, doesn't change toggle semantics or the underlying availability logic (which already handled this correctly via the parent-walk). (2) User asked whether Liqueur should split the same way - Campari and Aperol are both bitter aperitif-style liqueurs. Migration `20260816011432_liqueur_aperitif_hierarchy.sql` adds "Aperitif" (parenting Campari/Aperol) and, matching the Rum/Whiskey precedent of creating the grouping type before it's needed, an empty "Digestif" ready for Amaretto/Kahlúa/Chartreuse-type additions later; Triple Sec is neither and stays unparented. (3) User asked "where did the lemons go" - false alarm, not a bug: Lemon Juice/Lime Juice are exactly where the previous migration put them, under the renamed "Juice" category, which - per the user's own request that juices sort last - is now the very last section on the My Bar screen, easy to miss without scrolling all the way down. Confirmed via a direct table dump, no data lost.

**One more follow-up**: user clarified "oranges, lemons... berries" meant the whole fruit/garnish, not just juice - a real gap, since only the *juiced* form existed (Lemon Juice, Lime Juice); nothing represented a garnish like an orange twist or muddled berries. The spec's own suggested category list names "garnish" as distinct from "juice" (§8.2), and it didn't exist. Asked the user via `AskUserQuestion` whether to (a) add the category structurally now and defer content to step 12's batch import, (b) add a small starter set now, or (c) hold off entirely - since populating garnish types is real catalog *content* (naming/granularity decisions: "Orange" vs "Orange Peel", one "Berries" vs species-specific), which `AGENTS.md` calls out as batch-import's job, not something to hand-write into migrations repeatedly. User chose (b). Migration `20260816012630_garnish_category.sql` adds "Garnish" (sort_order 90, trailing with Bitters/Herb/Juice) with five starter types: Orange, Lemon, Lime, Cherry, Berries - deliberately small, not an attempt at a full garnish catalog.

## Implemented & verified

- `pnpm test` — 31/31 passing (unchanged, no domain logic touched this chunk).
- `pnpm build` succeeds — no errors.
- New migrations clean on `db advisors --type all`/`--type security` (only the now-familiar accepted WARNs).
- New `recipes` embed for `fetchCommunityRecipes` verified valid via direct REST call (200, not 400).
- **DB logic verified directly** (see above) — publish/unpublish happy path + unauthorized-rejection, and the tightened edit/delete predicate across all three owner_id cases.
- Scroll/loading-flash fixes **confirmed working by the user in the browser**.
- Taxonomy migrations verified directly against the live DB (category/type table dumps post-migration - Dark Rum/Bourbon reparented under Rum/Whiskey, Citrus types moved into Juice, Citrus row gone, Wine untouched, Campari/Aperol under Aperitif, new Garnish category with its 5 types present).
- **Not yet browser-verified**: publish/unpublish, recipe editing (as owner and as admin-on-classic), that an ordinary member genuinely cannot reach another member's recipe's edit form, and the new My Bar category order/sub-grouping (Spirit, Liqueur, and the new Garnish section).
- **No recipe references any Garnish type yet** - the five new types (Orange, Lemon, Lime, Cherry, Berries) exist in the catalog and are toggleable in My Bar, but e.g. the Negroni's orange peel is still only mentioned in its free-text prep steps, not modeled as a `recipe_components` row. Wiring existing classic recipes' garnishes to the new types (where it makes sense) is a reasonable follow-up, not done as part of this chunk.

## Remaining / not started

Step 12-13. Plus, carried over: no editor UI for substitution alternatives/hierarchy (step 8), no RLS/integration test harness for anything beyond what's been manually smoke-tested (step 8), `<datalist>` theming (step 6, user-accepted). Admin Overview/Invitations/Batch Import tabs still mock (step 12).

## Blockers / open questions

Not blocking, but worth raising with the user: a "Juice" and an unrelated, still-empty "Wine" ingredient category exist in the live DB with no migration behind them (found while running the taxonomy migration above). Someone/something created them outside the tracked migration history. Not investigated - just worked around (merged into "Juice" rather than colliding with it; left "Wine" alone).

## Decisions made & why

- **One `unpublish_recipe` function handles both the owner-self-service and admin-moderation cases**, distinguished internally by whether the caller is the owner, rather than two separate functions. The spec treats them as related but distinct permissions ("unpublish own recipe" vs "unpublish another user's recipe") - one function with an internal branch keeps the "what actually changes in the database" logic in one place instead of duplicated.
- **`moderation_status` only becomes `'unpublished_by_admin'` on the admin path**, staying `'active'` when an owner unpublishes their own work - this preserves the meaning of that field as "was this an admin moderation action," which matters if the UI ever wants to explain *why* something is private (e.g. distinguishing "you took this down" from "an admin removed it").
- **Removed the mock's "pending/approve/reject" workflow entirely rather than adapting it.** It never corresponded to real spec behavior (publishing is immediate, no pre-review queue) - keeping any trace of it would present a false workflow model to whoever uses the admin panel. Same reasoning applied to Overview's "Pending Review" card.
- **Verified the DB-level fix with real simulated identities before considering this chunk done**, rather than relying on code review alone for security-critical authorization logic. Used `set_config('request.jwt.claims', ...)` to fake `auth.uid()` in a `db query` session since that path otherwise has no JWT context at all.
- **Tightened admin edit/delete rights to the classic catalog only**, discovered while wiring up recipe editing. The original policy let an admin edit/delete any member's private recipe, which the spec explicitly says should be "No by default" - fixed rather than left as-is even though the gap predated this chunk and nothing in the UI had exercised it, since building the edit feature was exactly the moment this needed to be gotten right.
- **`updateRecipe()` doesn't roll back a partial failure**, unlike `createRecipe()` which deletes the whole row it just inserted. There's nothing sensible to roll back *to* for an update (the pre-edit state isn't held in memory as a restorable snapshot) - a failure surfaces the error and leaves the DB in whatever partial state the sequential calls reached, same honest limitation already accepted for `createRecipe()`'s components/tags steps.

Earlier decisions (still standing, trimmed here — see git history for step 2-10 notes): JS-only in `src/**` with `vite.config.ts` exempted; hosted Supabase; repo at `github.com/maxsoulfly/cocktail-library`; RLS policy shape (one policy per command, `to authenticated` explicit, `(select auth.uid())` wrapped, `coalesce(..., false)` around any comparison against a nullable column used in an authorization check); every `SECURITY DEFINER` function needs `revoke ... from public, anon, authenticated` explicitly; `useCatalog`/`useInventory`/`useRecipes`/`useLists` called exactly once in `AppShell` and shared via context; `Card` now correctly forwards `onClick`.

## Migrations / environment changes

Five new migrations this chunk, all applied via `supabase db push`: `20260815230002_recipe_publishing.sql` (publish/unpublish), `20260815231800_tighten_recipe_edit_scope.sql` (scopes admin recipe edit/delete to the classic catalog only), `20260816010047_category_order_and_spirit_hierarchy.sql` (category sort order, Citrus→Juice merge, Rum/Whiskey hierarchy), `20260816011432_liqueur_aperitif_hierarchy.sql` (Aperitif/Digestif hierarchy under Liqueur), and `20260816012630_garnish_category.sql` (new Garnish category with 5 starter types: Orange, Lemon, Lime, Cherry, Berries). No new environment variables.

## Tests / build checks last run

2026-08-16: `pnpm test` — 31/31 passing. `pnpm build` — no errors (multiple times across the chunk, before/after `pnpm format` each time). `npx supabase db push` — all five migrations applied (one required a fix-forward after a unique-constraint collision, see above). `npx supabase db advisors --linked --type all` / `--type security` — clean (familiar accepted WARNs only) after every migration. REST embed verified via `curl`. Publish/unpublish and tightened edit/delete predicates verified directly against the live DB with simulated auth contexts. All taxonomy migrations' end states verified with direct table dumps. Loading-flash and scroll fixes **confirmed by the user in-browser**; the rest of this chunk (publish/unpublish, recipe editing, My Bar taxonomy display) is not yet browser-verified.

## Exact next recommended action

Browser-verify what's accumulated and unverified: publish → unpublish from the detail page → publish again → unpublish from Admin → Moderation → edit a private recipe (including a long one) → as admin, edit a classic recipe → open My Bar and confirm Spirits sorts first, Bitters/Herb/Juice sort last, Dark Rum/Bourbon appear indented under Rum/Whiskey with a "Covered by" note when the parent itself isn't owned, and Campari/Aperol appear indented under Aperitif. Once confirmed, start step 12: admin catalog tools + JSON import. Per spec §12, this needs a copyable formatting prompt (generated from the same schema/constants used by the importer, not hand-written separately, so instructions can't drift from validation), a JSON paste + parse + validate + preview flow (additions/updates/duplicates/errors), and atomic-or-clearly-partial commit. Also folds in the rest of `AdminScreen`'s remaining mock tabs: real invitation generation/revocation (there's already a working manual-SQL path for this, documented in earlier chunks, that becomes real UI here), and real ingredient/product/glass/taste-tag/family management.

## Files/areas relevant to next action

`docs/Cocktail_Library_Development_Spec.md` §12 (batch import - the full validation/preview/duplicate-detection requirements) and §7.7 (admin area scope). `src/screens/AdminScreen.jsx` (Invitations and Batch Import tabs, still fully mock). New `src/schemas/` directory (per `AGENTS.md`'s target structure, not created yet - runtime validation schemas for import payloads). New `src/services/invitations.js` and `src/services/catalogAdmin.js`-style modules. A new migration for an invitation-generation `SECURITY DEFINER` function (same pattern as everything else protected in this project) if generation needs anything beyond a plain authenticated insert under the existing `invitations: admin manages` policy.
